<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <activeProfiles>
        <activeProfile>gitea-deploy</activeProfile>
    </activeProfiles>

    <profiles>
        <profile>
            <id>gitea-deploy</id>
            <repositories>
                <repository>
                    <id>central</id>
                    <url>https://repo.maven.apache.org/maven2</url>
                </repository>
                <repository>
                    <id>gitea-deploy</id>
                    <url>https://gitea.roboter5123.com/api/packages/frameboter/maven</url>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                </repository>
            </repositories>
        </profile>
    </profiles>

    <servers>
        <server>
            <id>gitea-deploy</id>
            <username>frameboter</username>
            <password>${env.ACCESS_TOKEN}</password>
        </server>
    </servers>

</settings>
